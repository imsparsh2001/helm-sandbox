apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubemmerce.fullname" . }}-test-connection"
  labels:
    {{- include "kubemmerce.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubemmerce.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
