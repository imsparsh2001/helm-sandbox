apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubemmerce.fullname" . }}
  labels:
    {{- include "kubemmerce.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubemmerce.selectorLabels" . | nindent 4 }}
